# Validated implementation plan — 17 September 2026

All four source documents were received, read completely and compared with the master prompt before implementation. Earlier missing-source status is closed. Their SHA-256 checksums matched the supplied originals. Raw research and its editorial notes are preserved.

## Resolved source and architecture decisions

- The official case requires a coherent profile → diagnosis → recommendations → comparison → roadmap → next action → progress journey and permits local persistence.
- Critical unknowns override contradictory READY examples in the research. Conditional English routes retain common academic/application gates. Reference costs retain their 2026–27 period, distinct from unknown 2027 totals.
- The user explicitly requested fresh official and forum research. The separately documented supplement resolves several gaps without overwriting the frozen reports. Forum anecdotes only supply discovery leads.
- Under the user's authorization to improve implementation independently, replace the initial Django/PostgreSQL/Tailwind proposal with Next.js Route Handlers, a pure TypeScript engine, versioned JSON and native CSS. This deploys on Vercel as one application without a second server or database account. It is an engineering deviation, not a claim that the initial stack was implemented.

## Deliverables and validation gates

| Workstream | Implementation | Gate |
| --- | --- | --- |
| Sources/data | Four original files, official supplement, 12-program dataset | Unique IDs, referenced facts exist, deterministic regeneration |
| Evidence/decisions | Typed DSL, evidence resolver, independent dimensions | Truth tables, UNKNOWN/conflicts, intake, costs, deadlines |
| Causal planning | Bounded recourse, same-pipeline simulation/diff | Immutability, timing, no invented SAT target, budget independence |
| Journey | Profile, diagnosis, cards, evidence, compare, shortlist, roadmap | Complete browser scenario, persistence and API validation |
| Design | Manrope/DM Sans, branching path motif, responsive dashboard | Desktop/mobile screenshots, keyboard dialogs, axe contrast checks |
| Delivery | README, architecture, limitations, Vercel | Production build and public URL verification |

The master prompt's 22 Definition of Done criteria are addressed by these gates. JSON fixtures replace the proposed database, and browser persistence replaces server sessions. Readiness means supported evaluated requirements are satisfied, never an admission prediction. The golden journey demonstrates three supported ready routes after explicit score changes and document declarations. Remaining gaps do not block the rest of the application.

Final executed checks are recorded in `validation.md`; launch and demo instructions are in the README. Team identities and separate video/slide submissions must be provided by the actual team.
